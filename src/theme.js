import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        brand: {
            1000: '#12161e', // Cor de fundo personalizada
        },
    },
});

export default theme;
